/**
 * The credit line, bottom-left on every public page.
 *
 * Fixed like the rest of the chrome, and — on Home — it stays put while the
 * About view is open, so the page keeps its frame and only the content inside
 * it changes.
 */
export default function Colophon() {
  return <p className="colophon">Designed and built by Aman</p>
}
